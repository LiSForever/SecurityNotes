#### 计划任务

目录

```txt
/var/spool/cron/{user}
/etc/crontab

# 文件名不能包括点号
/etc/cron.d/
/etc/cron.hourly/
```

/var/spool/cron/{user}和/etc/crontab

```txt
* * * * * ping -c 1 www.baidu.com
```

/etc/cron.d/

```
* * * * * root ping -c 1 www.baidu.com
```

/etc/cron.hourly/，下面放脚本